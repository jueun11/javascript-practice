console.log("index 파일 불러옴");
let randomColor = require("randomcolor"); // import the script
// *외부 패키지 모듈은 경로가 아니라 그냥 써주면댐
let color = randomColor(); // a hex code for an attractive color
console.log(color);
